using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Skill2 : MonoBehaviour
{
    //public Image chargeImgBG;
    //public Image chargeImg;

    //public GameObject shotPos;
    //public GameObject chargeShotPos;

    //public bool isCharge;
    //public bool isShoot;

    // Start is called before the first frame update
    void Start()
    {
        //chargeImgBG.gameObject.SetActive(false);
        //shotPos.gameObject.SetActive(false);
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.W))
        //{
        //    chargeImgBG.gameObject.SetActive(true);
        //    shotPos.gameObject.SetActive(true);
        //    isCharge = true;
        //    isShoot = false;
        //}
        //else if (Input.GetKeyUp(KeyCode.W))
        //{
        //    chargeImgBG.gameObject.SetActive(false);
        //    shotPos.gameObject.SetActive(false);
        //    isCharge = false;
        //    isShoot = true;
        //}

        //if (isCharge)
        //{
        //    chargeImg.fillAmount += Time.deltaTime * 0.5f;
        //}
        //else
        //{
        //    chargeImg.fillAmount -= Time.deltaTime * 2.0f;

        //    if(chargeImg.fillAmount <= 0.0f)
        //        chargeImgBG.gameObject.SetActive(false);
        //}

        //if (isShoot)
        //{
        //    GameObject charge_prefab = (GameObject)Instantiate(Resources.Load("Prefab/Charge_ATT")) as GameObject;
        //    charge_prefab.transform.position = chargeShotPos.transform.position;
        //    isShoot = false;
        //}
    }
}
